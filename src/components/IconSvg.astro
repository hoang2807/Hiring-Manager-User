---
interface Props {
  icon: string;
}

const { icon } = Astro.props;
const { default: innerHTML } = await import(`../assets/icon/${icon}.svg?raw`);
---

<Fragment set:html={innerHTML} set:class="w-20 h-20 md:w-32 md:h-32" />
